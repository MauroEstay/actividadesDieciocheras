<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="<%= tasks_path %>">App18</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">

    <ul class="navbar-nav ml-auto">
      <% if user_signed_in? %>
          <a class="nav-link" href="#"><%= current_user.email %><span class="sr-only">(current)</span></a>
          <a class="nav-link" href="#">Tareas Completadas <%= @userTask.count %> / <%= @tasks.count %><span class="sr-only">(current)</span></a>
          <%= link_to 'Salir', destroy_user_session_path, method: :DELETE, class: 'nav-item nav-link justify-content-end' %>
      <% else %>
        <li class="nav-item">
          <a class="nav-item nav-link active justify-content-end" href="<%= new_user_session_path %>"> Ingresar <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
          <a class="nav-item nav-link justify-content-end" href=" <%= new_user_registration_path %> "> Registrarse </a>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
